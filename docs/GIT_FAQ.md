
Этот FAQ содержит все основные команды Git, которые используются при работе с проектом.

---

## 📋 Содержание

1. [Первоначальная настройка](#первоначальная-настройка)
2. [Проверка статуса](#проверка-статуса)
3. [Работа с изменениями](#работа-с-изменениями)
4. [Коммиты](#коммиты)
5. [Работа с удалённым репозиторием](#работа-с-удалённым-репозиторием)
6. [Ветки](#ветки)
7. [Откат изменений](#откат-изменений)
8. [Решение проблем](#решение-проблем)
9. [Работа с GitHub](#работа-с-github)

---

## 🔧 Первоначальная настройка

### Настройка пользователя (первый раз)

```bash
# Установить ваше имя (глобально для всех репозиториев)
git config --global user.name "Ваше Имя"

# Установить ваш email (глобально для всех репозиториев)
git config --global user.email "your.email@example.com"

# Проверить текущие настройки
git config --global --list
```

### Инициализация нового репозитория

```bash
# Создать новый Git репозиторий в текущей папке
git init

# Это создаст скрытую папку .git с историей проекта
```

---

## 📊 Проверка статуса

### Проверить текущий статус

```bash
# Показать какие файлы изменены, добавлены, удалены
git status

# Краткий вывод (одна строка на файл)
git status -s
```

**Что показывает:**
- `Untracked files` - новые файлы, которые Git не отслеживает
- `Changes not staged for commit` - изменённые файлы, которые не добавлены в staging
- `Changes to be committed` - файлы готовые к коммиту (в staging area)

### Просмотр изменений

```bash
# Показать различия между рабочей директорией и staging area
git diff

# Показать различия в staging area (готовые к коммиту)
git diff --staged
# или
git diff --cached

# Показать изменения конкретного файла
git diff path/to/file.tsx

# Краткий список изменённых файлов
git diff --name-only
```

### История коммитов

```bash
# Показать историю коммитов
git log

# Краткая история (одна строка на коммит)
git log --oneline

# История с графиком веток
git log --oneline --graph --all

# Последние N коммитов
git log -n 5
```

---

## 📝 Работа с изменениями

### Добавление файлов в staging

```bash
# Добавить все изменённые и новые файлы
git add .

# Добавить конкретный файл
git add path/to/file.tsx

# Добавить все файлы с расширением .tsx
git add *.tsx

# Добавить все файлы в конкретной папке
git add components/

# Интерактивный режим (выбрать файлы по одному)
git add -i
# или
git add -p
```

### Удаление файлов из staging

```bash
# Убрать файл из staging (но оставить изменения в файле)
git reset path/to/file.tsx

# Убрать все файлы из staging
git reset

# Убрать из staging и удалить изменения в файле (ОСТОРОЖНО!)
git reset --hard path/to/file.tsx
```

### Отмена изменений в файлах

```bash
# Отменить изменения в файле (восстановить из последнего коммита)
git checkout -- path/to/file.tsx

# Или (новая команда в Git 2.23+)
git restore path/to/file.tsx

# Отменить все изменения во всех файлах (ОСТОРОЖНО!)
git checkout -- .
# или
git restore .
```

---

## 💾 Коммиты

### Создание коммита

```bash
# Создать коммит с сообщением
git commit -m "Описание изменений"

# Создать коммит с подробным сообщением (откроется редактор)
git commit

# Добавить все изменения и создать коммит сразу (без git add)
git commit -am "Описание изменений"

# Создать коммит с несколькими строками сообщения
git commit -m "Короткое описание" -m "Подробное описание изменений"
```

### Изменение последнего коммита

```bash
# Изменить сообщение последнего коммита
git commit --amend -m "Новое сообщение"

# Добавить файлы в последний коммит
git add забытый_файл.tsx
git commit --amend --no-edit
```

**Примеры хороших сообщений коммитов:**
- `"Fix: исправлена ошибка в Header.tsx"`
- `"Add: добавлен компонент ReferralTree"`
- `"Update: обновлены зависимости в package.json"`
- `"Refactor: оптимизирован код в lib/sdk/fetch.ts"`

---

## 🌐 Работа с удалённым репозиторием

### Проверка удалённых репозиториев

```bash
# Показать все удалённые репозитории
git remote -v

# Показать подробную информацию об origin
git remote show origin
```

### Подключение к удалённому репозиторию

```bash
# Добавить удалённый репозиторий (называется origin)
git remote add origin https://github.com/username/repository.git

# Изменить URL удалённого репозитория
git remote set-url origin https://github.com/username/new-repository.git

# Использовать SSH вместо HTTPS
git remote set-url origin git@github.com:username/repository.git

# Использовать Personal Access Token (для приватных репозиториев)
git remote set-url origin https://YOUR_TOKEN@github.com/username/repository.git
```

### Получение изменений из удалённого репозитория

```bash
# Скачать изменения из удалённого репозитория (не объединять)
git fetch origin

# Скачать и объединить изменения (pull = fetch + merge)
git pull origin master

# Или просто (если ветка отслеживается)
git pull

# Получить все ветки из удалённого репозитория
git fetch --all
```

### Отправка изменений в удалённый репозиторий

```bash
# Отправить коммиты в удалённый репозиторий
git push origin master

# Установить upstream и отправить (первый раз)
git push -u origin master

# Отправить текущую ветку
git push

# Принудительная отправка (ОСТОРОЖНО! Перезаписывает удалённую ветку)
git push --force
# или
git push -f
```

---

## 🌿 Ветки

### Работа с ветками

```bash
# Показать все ветки
git branch

# Показать все ветки (включая удалённые)
git branch -a

# Создать новую ветку
git branch new-branch-name

# Переключиться на ветку
git checkout branch-name

# Создать и переключиться на новую ветку (одной командой)
git checkout -b new-branch-name

# Или (новая команда в Git 2.23+)
git switch -c new-branch-name

# Удалить ветку (локально)
git branch -d branch-name

# Удалить ветку принудительно
git branch -D branch-name

# Удалить ветку на удалённом репозитории
git push origin --delete branch-name
```

### Слияние веток

```bash
# Переключиться на основную ветку (например master)
git checkout master

# Объединить ветку в текущую ветку
git merge feature-branch

# Объединить без создания merge commit (fast-forward)
git merge --ff-only feature-branch
```

---

## ⏪ Откат изменений

### Откат коммитов

```bash
# Отменить последний коммит (но оставить изменения в файлах)
git reset --soft HEAD~1

# Отменить последний коммит и убрать изменения из staging
git reset HEAD~1
# или
git reset --mixed HEAD~1

# Отменить последний коммит и удалить все изменения (ОСТОРОЖНО!)
git reset --hard HEAD~1

# Отменить несколько коммитов (последние N)
git reset --soft HEAD~3  # отменить 3 последних коммита
```

### Создать новый коммит, отменяющий изменения

```bash
# Создать коммит, который отменяет изменения указанного коммита
git revert COMMIT_HASH

# Отменить последний коммит через revert
git revert HEAD
```

---

## 🔍 Решение проблем

### Конфликты при merge

```bash
# Если возникли конфликты при pull/merge:

# 1. Посмотреть конфликтующие файлы
git status

# 2. Открыть файлы и разрешить конфликты вручную
# Git помечает конфликты так:
# <<<<<<< HEAD
# ваш код
# =======
# код из другой ветки
# >>>>>>> branch-name

# 3. После разрешения конфликтов:
git add resolved-file.tsx

# 4. Завершить merge
git commit
```

### Отмена merge в процессе

```bash
# Если начали merge, но хотите отменить
git merge --abort
```

### Потерянные изменения

```bash
# Найти "потерянные" коммиты
git reflog

# Восстановить коммит из reflog
git checkout COMMIT_HASH
```

### Очистка проекта

```bash
# Удалить неотслеживаемые файлы (dry-run, показать что будет удалено)
git clean -n

# Удалить неотслеживаемые файлы
git clean -f

# Удалить неотслеживаемые файлы и папки
git clean -fd
```

---

## 🚀 Работа с GitHub

### Клонирование репозитория

```bash
# Клонировать репозиторий через HTTPS
git clone https://github.com/username/repository.git

# Клонировать через SSH
git clone git@github.com:username/repository.git

# Клонировать в конкретную папку
git clone https://github.com/username/repository.git my-folder

# Клонировать только конкретную ветку
git clone -b branch-name https://github.com/username/repository.git
```

### Создание репозитория на GitHub

1. Откройте https://github.com/new
2. Укажите имя репозитория
3. Выберите Public/Private
4. **НЕ** добавляйте README, .gitignore, license (если они уже есть локально)
5. Нажмите "Create repository"
6. Следуйте инструкциям на странице

### Первая отправка кода на GitHub

```bash
# Если репозиторий уже инициализирован локально:
git remote add origin https://github.com/username/repository.git
git branch -M master  # переименовать ветку в master (если нужно)
git push -u origin master
```

### Personal Access Token (для приватных репозиториев)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен

**Использование токена:**

```bash
# Встроить токен в URL (временно)
git remote set-url origin https://YOUR_TOKEN@github.com/username/repository.git

# Или при push ввести токен как пароль (username - ваш username, password - токен)
git push origin master
```

**⚠️ Важно:** После использования токена, рекомендуется удалить его из URL:
```bash
git remote set-url origin https://github.com/username/repository.git
```

### SSH ключи (рекомендуется)

```bash
# Создать SSH ключ (если ещё нет)
ssh-keygen -t ed25519 -C "your.email@example.com"

# Показать публичный ключ (скопируйте его)
cat ~/.ssh/id_ed25519.pub

# Добавить ключ на GitHub:
# Settings → SSH and GPG keys → New SSH key → Вставить ключ

# Проверить подключение
ssh -T git@github.com

# Использовать SSH URL
git remote set-url origin git@github.com:username/repository.git
```

---

## 📚 Полезные комбинации команд

### Полный цикл работы

```bash
# 1. Проверить статус
git status

# 2. Посмотреть изменения
git diff

# 3. Добавить файлы
git add .

# 4. Создать коммит
git commit -m "Описание изменений"

# 5. Получить изменения с сервера
git pull

# 6. Отправить изменения
git push
```

### Быстрая проверка перед коммитом

```bash
# Проверить статус и diff одновременно
git status && git diff
```

### Создание .gitignore

```bash
# Если забыли добавить файл в .gitignore и он уже в репозитории:
# 1. Удалить из Git (но оставить файл)
git rm --cached file.txt

# 2. Добавить в .gitignore
echo "file.txt" >> .gitignore

# 3. Закоммитить
git add .gitignore
git commit -m "Add file.txt to .gitignore"
```

---

## 🎯 Частые сценарии

### Я случайно закоммитил секретные данные

```bash
# 1. Удалить из последнего коммита
git rm --cached .env.local
git commit --amend

# 2. Добавить в .gitignore
echo ".env.local" >> .gitignore
git add .gitignore
git commit -m "Add .env.local to .gitignore"

# 3. Если уже отправили на GitHub:
# Немедленно смените секреты! Git история останется на GitHub!
# Рассмотрите использование git filter-branch или BFG Repo-Cleaner
```

### Я хочу отменить все локальные изменения

```bash
# ВНИМАНИЕ: Это удалит все несохранённые изменения!
git reset --hard HEAD
git clean -fd
```

### Я хочу увидеть, что изменилось в конкретном коммите

```bash
# Показать изменения в коммите
git show COMMIT_HASH

# Показать только список файлов
git show --name-only COMMIT_HASH

# Показать статистику изменений
git show --stat COMMIT_HASH
```

### Я хочу перенести изменения из одной ветки в другую

```bash
# Способ 1: Cherry-pick (взять конкретный коммит)
git checkout target-branch
git cherry-pick COMMIT_HASH

# Способ 2: Merge (объединить всю ветку)
git checkout target-branch
git merge source-branch
```

---

## 🔐 Безопасность

### Никогда не коммитьте:

- `.env` файлы
- Файлы с паролями и токенами
- Приватные ключи (wallet JSON файлы)
- `node_modules/`
- Build артефакты

### Проверить, что в репозитории нет секретов:

```bash
# Поиск потенциальных секретов
git log --all --full-history --source -- "*.env*"
git log --all --full-history --source -- "*key*"
git log --all --full-history --source -- "*secret*"
```

---

## 📖 Дополнительные ресурсы

- Официальная документация: https://git-scm.com/doc
- Интерактивный туториал: https://learngitbranching.js.org/
- GitHub Guides: https://guides.github.com/

---

## 🆘 Быстрая справка

```bash
# Помощь по любой команде
git help COMMAND
# Например:
git help commit
git help push

# Список всех команд
git help --all
```

---

**Удачной работы с Git! 🚀**

